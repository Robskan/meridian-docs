---
title: "Set prefix"
description: "Change the bot command prefix globally or per server."
icon: "at-sign"
---

The **Set prefix** block lives in the **Settings** palette category. Change the bot command prefix globally or per server.

## What it does

Change the bot command prefix globally or per server.

## Configuration

Open the block in the builder sidebar to configure these fields.

### `scope`

**Type:** `string`. Required.

Apply prefix globally or only in the current server. Accepted values: `server`, `global`.

### `new_prefix`

**Type:** `string`. Required.

Character(s) users type before prefix commands (for example `!` or `?`).

## See also

- [Settings](/builder/action-blocks/settings) — Browse all blocks in the Settings category.