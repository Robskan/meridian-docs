---
title: "Event"
description: "Starts when Discord emits this client event or when a webhook hits your bot."
icon: "radio"
---

Event flows react to Discord activity or inbound webhooks instead of user commands.

## What it does

Subscribes to a discord.js client event (such as `messageCreate` or `guildMemberAdd`) or exposes an HTTP webhook path. When the event fires, blocks connected below run in order.

## Common uses

<CardGroup cols={2}>
  <Card title="Welcome messages" icon="user-plus">
    Run on `guildMemberAdd` to greet new members.
  </Card>

  <Card title="Auto moderation" icon="shield">
    React to `messageCreate` with filters and actions.
  </Card>

  <Card title="External triggers" icon="webhook">
    Start flows from outside Discord via webhook POST requests.
  </Card>
</CardGroup>

<Note>
  This block is placed automatically by the builder. You cannot add it from the block palette.
</Note>

## Configuration

Open the block in the builder sidebar to configure these fields.

<ParamField path="friendly_name" type="string" required>
  Dashboard title for this event flow.
</ParamField>

<ParamField path="friendly_description" type="string">
  Optional notes shown on the card and in your event list.
</ParamField>

<ParamField path="client_event" type="string" required>
  discord.js event name (for example `messageCreate`, `voiceStateUpdate`).
</ParamField>

<ParamField path="trigger_on_users" type="boolean">
  Allow human accounts to trigger the flow.
</ParamField>

<ParamField path="trigger_on_bots" type="boolean">
  Allow other bot accounts to trigger the flow.
</ParamField>

<ParamField path="trigger_on_webhooks" type="boolean">
  Allow channel webhook posts to trigger the flow.
</ParamField>

<Tip>
  For webhook events, configure path suffix and optional `mrdn-key` authentication in the block sidebar.
</Tip>

<Warning>
  This bot's own messages never trigger event flows, even when author filters are broad.
</Warning>

## Related blocks

<CardGroup cols={2}>
  <Card title="Event flow" icon="cube" href="/builder/action-blocks/event-flow">
    Browse all blocks in the Event flow category.
  </Card>
</CardGroup>