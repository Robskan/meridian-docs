---
title: "Edit a Channel (incl. Threads)"
description: "Modify channel name, topic, or NSFW settings."
---

The **Edit a Channel (incl. Threads)** block lives in the **Channels** palette category. Modify channel name, topic, or NSFW settings.

## What it does

Changes channel name, topic, or NSFW flag for channels and threads.







## Configuration

Open the block in the builder sidebar to configure these fields.

<ParamField path="channel_id" type="channel" required>
  Target channel, variable, or Create Channel block reference.
</ParamField>

<ParamField path="new_name" type="string">
  Updated name.
</ParamField>

<ParamField path="new_topic" type="string">
  Updated topic.
</ParamField>

<ParamField path="nsfw" type="boolean">
  Age-restricted flag.
</ParamField>











## Related blocks

<CardGroup cols={2}>
  <Card title="Channels" icon="cube" href="/builder/action-blocks/channels">
    Browse all blocks in the Channels category.
  </Card>
</CardGroup>
