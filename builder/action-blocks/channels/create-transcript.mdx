---
title: "Create a Transcript"
description: "Export channel history to a hosted HTML transcript."
---

Create a Transcript exports channel history to a hosted HTML page on Meridian.

## What it does

Collects messages, renders a shareable transcript, and stores URLs (and optional passwords) in flow variables.

## Configuration

Open the block in the builder sidebar to configure these fields.

<ParamField path="channel_source" type="string" required>
  undefined

  Options: `current`, `specific`, `variable`, `block`.
</ParamField>

<ParamField path="transcript_name" type="string">
  Download filename and page title.
</ParamField>

<ParamField path="output_prefix" type="string">
  Variable prefix (`{vars.ticket_log_url}`, etc.).
</ParamField>

<ParamField path="access_mode" type="string">
  undefined

  Options: `public`, `roles`, `password`, `roles_and_password`.
</ParamField>

<Tip>
  Use role-gated transcripts for ticket logs visible only to staff.
</Tip>

## Related blocks

<CardGroup cols={2}>
  <Card title="Channels" icon="cube" href="/builder/action-blocks/channels">
    Browse all blocks in the Channels category.
  </Card>
</CardGroup>