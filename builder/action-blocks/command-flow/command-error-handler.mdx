---
title: "Error handler"
description: "Runs when this command throws or fails."
icon: "alert-triangle"
---

The Error handler sits beside your Command block and runs when any block in the command flow throws or fails at runtime.

## What it does

Gives you a dedicated branch for custom failure messages, logging, or recovery steps. If you disable it, Meridian shows the built-in flow error card in Discord instead.

## Common uses

<CardGroup cols={2}>
  <Card title="Friendly errors" icon="message">
    Send a custom embed explaining what went wrong instead of a generic card.
  </Card>

  <Card title="Staff alerts" icon="bell">
    Mirror errors to a Discord webhook for your moderation or dev channel.
  </Card>

  <Card title="Silent failures" icon="eye-closed">
    Hide error details from regular members while still notifying owners.
  </Card>
</CardGroup>

<Note>
  This block is placed automatically by the builder. You cannot add it from the block palette.
</Note>

## Configuration

Open the block in the builder sidebar to configure these fields.

<ParamField path="disable_error_handler" type="boolean">
  When on, failures skip blocks under this anchor and only show the built-in flow error card.
</ParamField>

<ParamField path="errors_visible_owner_cowork_only" type="boolean">
  Hides flow error messages from everyone except the bot owner and coworkers with builder access.
</ParamField>

<ParamField path="error_webhook_enabled" type="boolean">
  Post a copy of each error to a Discord webhook URL.
</ParamField>

<ParamField path="error_webhook_url" type="string">
  Webhook URL. Required when webhook delivery is enabled.
</ParamField>

<ParamField path="error_webhook_username" type="string">
  Optional display name for webhook messages.
</ParamField>

<Tip>
  Connect a Send a Message block under the Error handler to explain failures in plain language.
</Tip>

## Related blocks

<CardGroup cols={2}>
  <Card title="Command flow" icon="cube" href="/builder/action-blocks/command-flow">
    Browse all blocks in the Command flow category.
  </Card>
</CardGroup>